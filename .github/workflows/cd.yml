name: CD

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]

concurrency:
  group: cd-main
  cancel-in-progress: false

jobs:
  migrate:
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install neonctl
        run: npm install -g neonctl

      - name: Fetch connection string from Neon
        env:
          NEON_API_KEY: ${{ secrets.NEON_API_KEY }}
          NEON_PROJECT_ID: ${{ secrets.NEON_PROJECT_ID }}
          NEON_DATABASE_URL_FALLBACK: ${{ secrets.NEON_DATABASE_URL }}
        run: |
          set -euo pipefail
          if [ -n "${NEON_API_KEY:-}" ] && [ -n "${NEON_PROJECT_ID:-}" ]; then
            echo "Fetching connection string from Neon using API key..."
            export NEON_DATABASE_URL=$(neonctl connection-string production --project-id "${NEON_PROJECT_ID}")
          elif [ -n "${NEON_DATABASE_URL_FALLBACK:-}" ]; then
            echo "Using fallback NEON_DATABASE_URL secret..."
            export NEON_DATABASE_URL="${NEON_DATABASE_URL_FALLBACK}"
          else
            echo "ERROR: Neither Neon API credentials nor fallback connection string available"
            exit 1
          fi
          echo "NEON_DATABASE_URL=${NEON_DATABASE_URL}" >> $GITHUB_ENV

      - name: Sync backend dependencies
        run: uv sync

      - name: Capture Alembic heads output
        run: |
          set -euo pipefail
          uv run alembic heads 2>&1 | tee alembic-heads.txt

      - name: Capture Alembic current output
        run: |
          set -euo pipefail
          uv run alembic current 2>&1 | tee alembic-current.txt

      - name: Abort on multiple Alembic heads
        run: |
          set -euo pipefail
          head_count=$(grep -Ec '\(head\)$' alembic-heads.txt || true)
          if [ "$head_count" -gt 1 ]; then
            echo 'ERROR: multiple alembic heads detected, aborting deploy'
            exit 1
          fi

      - name: Capture Alembic history output
        run: |
          set -euo pipefail
          uv run alembic history 2>&1 | tee alembic-history.txt

      - name: Run Alembic migrations
        run: |
          set -euo pipefail
          uv run alembic upgrade head
          uv run alembic current | tee alembic-current-final.txt
          if ! grep -q '(head)' alembic-current-final.txt; then
            echo 'ERROR: Alembic did not upgrade to head. Aborting deploy.'
            exit 1
          fi

      - name: Upload Alembic history artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: alembic-history
          path: backend/alembic-history.txt
          if-no-files-found: warn

  deploy-backend:
    needs: migrate
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    env:
      FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Validate Fly configuration
        run: flyctl config validate

      - name: Deploy backend to Fly.io
        run: flyctl deploy --remote-only --wait-timeout 300

  smoke:
    needs: deploy-backend
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test production health endpoint
        run: |
          set -euo pipefail

          if ! response=$(curl --fail --max-time 30 --retry 5 --retry-delay 10 https://minis-api.fly.dev/api/health); then
            echo 'Smoke test failed. To rollback: fly deploy rollback -a minis-api (WARNING: DB migrations already applied — verify before rolling back)'
            exit 1
          fi

          echo "$response"

          if ! echo "$response" | grep -q 'status'; then
            echo 'Smoke test failed. To rollback: fly deploy rollback -a minis-api (WARNING: DB migrations already applied — verify before rolling back)'
            exit 1
          fi
